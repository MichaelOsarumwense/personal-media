{
  "name": "personal-media-selenium-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Selenium WebDriver + Mocha UI tests for Personal Media app (JS)",
  "type": "module",
  "scripts": {
    "test": "mocha",
    "test:smoke": "mocha --grep '@smoke'",
    "test:headed": "cross-env HEADLESS=false npm test",
    "report": "mochawesome-merge artifacts/mochawesome/*.json > artifacts/mochawesome/merged.json && marge artifacts/mochawesome/merged.json -f index -o artifacts/mochawesome"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pino": "^9.4.0",
    "selenium-webdriver": "^4.25.0"
  },
  "devDependencies": {
    "chai": "^5.3.0",
    "cross-env": "^7.0.3",
    "mocha": "^10.7.3",
    "mocha-junit-reporter": "^2.2.1",
    "mocha-multi-reporters": "^1.5.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
